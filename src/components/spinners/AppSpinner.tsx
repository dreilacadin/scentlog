import { Center, Container, Loader } from "@mantine/core";

const AppSpinner = () => {
  return (
    <Container>
      <Center>
        <Loader />
      </Center>
    </Container>
  );
};

export default AppSpinner;
